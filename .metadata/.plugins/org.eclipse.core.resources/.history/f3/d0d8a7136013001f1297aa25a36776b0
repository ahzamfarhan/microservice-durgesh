package com.lcwd.userservice.interceptors;



public RestTemplateInterceptor implements ClientHttpRequestInterceptor {

	private OAuth2AuthorizedClientManager manager;

	RestTemplateInterceptor(OAuth2AuthorizedClientManager manager) {
		this.manager=manager;
	}

	/*
	@Override
	public ClientHttpResponse intercept(HttpRequest request, byte[] body, ClientHttpRequestExecution execution) throws .... {

		String token = manager.authorize(OAuth2AuthorizeRequest.withClientRegistrationId("my-internal-client").principal("internal2").build()).getAccessToken().getTokenValue();

		request.getHeaders().addHeader("Authorization", "Bearer " + token);


		return exection.execute(request, body);
	}
	*/

} 

